function stringLength(first,second,thir){
    let firstLength = first.length;
    let secondLength = second.length
    let thirdLength = thir.length;
    
    let sum = firstLength + secondLength + thirdLength
    
    let average = Math.round(sum/3);
    console.log(sum);
    console.log(average);
    
    }